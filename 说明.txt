这是本人这个学期的大作业，手写STL代码，分别是可合并优先队列（左偏树），map（avl平衡树），deque（块状链表）。
ppt是作业要求，请看提高组部分。
可合并优先队列：
    1.实现所有堆的基本操作；
	2.实现O(logn)的合并操作；
map：
    1.所有类似stl_map的功能；
	2.avl平衡化；
	3.迭代器中序遍历（从小到大）；
deque（double end queue)：
    1.O(1)的前后端插入删除；
	2.O(√n)的随机插入删除；
	3.O(√n)的随机访问；
公用头文件主要是一些用于测试的自定义类，以及异常抛出。
写的不好，还望见谅。